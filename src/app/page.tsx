import ImageProvider from "@/components/ImageProvider";
import React from "react";

const Home = () => {
  return (
    <div className="h-screen">
      <ImageProvider />
    </div>
  );
}

export default Home;
